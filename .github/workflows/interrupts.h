#ifndef INTERRUPTS_H
#define INTERRUPTS_H

// Declare the function to handle timer interrupts
void timer_interrupt_handler();

// Declare a function to initialize the timer interrupt
void setup_timer_interrupt();

// Declare any other interrupt handling functions you may need

#endif
